Расширьте разработанную на с. 139 реализацию паттерна фабрика, 
чтобы сделать ее безопаснее ценой некоторой утраты абстрактности. Обновите реализацию, чтобы потребитель во время компиляции знал, что вызов Shoe.create('boot') возвращает Boot, а вызов 
Shoe.create('balletFlat') — BalletFlat (вместо ситуации, при которой 
оба возвращают Shoe).